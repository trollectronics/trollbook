#include <draw/bitmap.h>

DrawBitmap *draw_bitmap_new(int x, int y) {
	
}

void draw_bitmap_free(DrawBitmap *bitmap) {
	
}

void draw_bitmap_move(DrawBitmap *bitmap) {
	
}

void draw_bitmap_draw(DrawBitmap *bitmap) {
	
}
